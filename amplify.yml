version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd game
        - npm ci
        - cd ..
    build:
      commands:
        - cd game
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*